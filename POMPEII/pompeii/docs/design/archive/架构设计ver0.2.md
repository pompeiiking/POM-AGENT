Pompeii - Agent 通用模块架构设计 ver0.2
=========================================

一、设计哲学
-----------

1. Agent 的本质是"询问器"，不是流水线节点。
   模型坐在中心反复推理："信息够不够？不够就调工具补。够了就回复。"
   这是 ReAct 模式（Reason → Act → Observe → Loop）。

2. 架构推导方法：双向推导。
   - 从模型输入接口（Context Window）倒推 → 推出所有感知侧模块
   - 从模型输出接口（Tool Call / 文本回复）正推 → 推出所有执行侧模块
   每个模块的存在都有明确理由：要么供给 Context Window，要么承接模型决策。

3. 微内核 + 读写分级控制。
   核心只做路由和协调，处理模块同级并列，共享服务按需供给。


二、整体架构
-----------

┌──────────────────────────────────────────────────────────┐
│                      Agent 通用模块                        │
│                                                          │
│  ┌──────────────────── AgentPort ────────────────────┐   │
│  │  统一对外接口                                      │   │
│  │  消息类型:   
│  │                             
│  └──────────────────────┬───────────────────────────┘   │
│                         │                               │
│  ┌──────────────────────┼───────────────────────────┐   │
│  │                  核心部（微内核）                    │   │
│  │                                                    │   │
│  │  · 处理模块间消息路由                               │   │
│  │  · Agent Loop 循环控制与终止判断                     │   │
│  │  · 资源写操作守门                                   │   │
│  │  · 资源读操作事件感知（异步）                        │   │
│  │  · 会话生命周期管理                                 │   │
│  │                                                    │   │
│  │  路由规则:                                          │   │
│  │  接口部→组装部→模型部→[工具部→组装部→模型部]→接口部  │   │
│  │                       ^^^^^^^^^^^^^^^^             │   │
│  │                        循环体(可重复)               │   │
│  └──┬────────┬────────┬────────┬─────────────────────┘   │
│     │        │        │        │                         │
│     ▼        ▼        ▼        ▼                         │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                    │
│  │接口部│ │组装部│ │模型部│ │工具部│  ← 四个处理模块同级  │
│  └──────┘ └──────┘ └──────┘ └──────┘                    │
│     │        │        │        │                         │
│     ▼        ▼        ▼        ▼  （读: 直连+上报）      │
│  ┌────────────────────────────────────────────────┐      │
│  │              共享服务层                          │      │
│  │  提示词配置  Skill注册表  信息缓存  长期存储      │      │
│  └────────────────────────────────────────────────┘      │
│                                                          │
│  ┌────────────────────────────────────────────────┐      │
│  │            安全策略模块（横切）                   │      │
│  └────────────────────────────────────────────────┘      │
└──────────────────────────────────────────────────────────┘


三、处理模块详解
---------------

四个处理模块地位相同，由核心部在它们之间路由消息。

3.1 接口部
  职责: Agent 对外通信的唯一出入口
  功能:
    · 输入适配: 外部消息 → 标准内部格式（多模态归一化、元数据提取）
    · 输出适配: 模型回复 → 外部格式（平台Markdown方言、消息分片、流式转发）
    · 安全过滤: 输入长度限制、频率限制、基础注入检测

3.2 组装部
  职责: Context Window 的唯一构建者和守门人
  两条路径:
    · 完整组装路径（首次）:
      从提示词配置读 System Prompt
      从 Skill 注册表读工具声明 + Skill 描述
      从信息缓存读会话历史
      从长期存储检索相关记忆（用用户原话作为检索 key）
      Token 预算计算 + 裁剪
      → 输出完整 Context Window
    · 追加路径（循环中工具结果回注时）:
      格式化工具返回结果
      Token 预算检查
      如果超限 → 压缩历史部分腾出空间
      → 输出更新后的 Context Window

  关键设计: 组装过程是算法性的，不需要大模型参与。
  用户的原话本身就是检索 key，无需"智能"判断该搜什么。

3.3 模型部
  职责: LLM 调用与输出解析
  功能:
    · 模型选择: 根据会话类型/任务复杂度选择模型
    · API 调用: 将 Context Window 发送给模型
    · 流式处理: 支持流式 token 输出
    · 输出解析: 判断输出是 tool_call 还是文本回复
    · Failover: 主模型失败时切换备用模型

3.4 工具部
  职责: 执行模型发出的 tool_call
  功能:
    · FC 执行器: 执行内部工具（Function Calling）
    · MCP 客户端: 连接外部 MCP Server 发起远端工具调用
    · 沙箱隔离: 危险操作在容器中执行，按会话创建/销毁
    · 结果处理: 格式化 + 截断过长的工具返回结果

  Workflow 封装: 大型多步骤确定性流程封装为 Workflow，
  作为一个 Tool 注册使用，模型调用时按预设流程执行。


四、共享服务层详解
-----------------

共享服务不参与处理流程，是被处理模块按需查询的数据/能力提供者。

4.1 提示词配置
  生命周期: 系统级，部署时写好基本不变
  内容: System Prompt 模板、人格定义（SOUL.md）、行为规则、输出格式约束
  被谁读: 组装部
  说明: 定义"模型应该表现成什么样"——身份问题

4.2 Skill 注册表
  生命周期: 系统级，运行时可热更新（加载/禁用/重载）
  内容: 每个 Skill 包含三部分打包在一起——
    (1) 描述文本（SKILL.md）→ 告诉模型该能力存在及使用策略
    (2) 可执行函数（tools）→ 实际的执行逻辑
    (3) 元数据配置 → 参数 Schema、权限要求、沙箱策略
  被谁读: 组装部（读描述+Schema）、工具部（读执行函数+沙箱配置）
  说明: 定义"模型能做什么事"——能力问题
         Skill ≠ Tool。Tool 是原子操作，Skill 是能力包。
         增删 Skill 只需改注册表，各消费模块自动生效。

  提示词配置和 Skill 注册表必须分离:
    · 变更节奏不同（提示词几乎不动，Skill 可动态增删）
    · 消费者不同（提示词只有组装部读，Skill 两个模块都读）
    · 关注点不同（身份 vs 能力）

4.3 信息缓存
  生命周期: 会话级，会话开始时创建，结束时归档或销毁
  内容: 当前会话的完整交互记录（用户消息、模型输出、工具调用及结果）
  被谁读: 组装部
  被谁写: 循环引擎每轮追加（写操作经核心）
  说明: 本质是 Agent Loop 运行时的工作记忆

4.4 长期存储
  生命周期: 持久级，跨会话存在
  内容: 向量索引 + BM25 关键词索引（混合检索）、对话归档、用户偏好、长期记忆
  被谁读: 组装部（检索相关记忆）
  被谁写: 会话结束时归档（写操作经核心）
  说明: 检索策略为混合检索（向量相似度 + BM25 关键词匹配），
       用用户原话做查询，无需大模型介入。


五、资源访问规则
---------------

规则1: 处理模块之间的通信       → 经核心路由（中介）
规则2: 资源写操作              → 经核心执行（中介）
规则3: 低/中敏感资源的读操作    → 直连 + 事件上报（感知）
规则4: 高敏感资源的读操作       → 经核心审批（中介）

读资源敏感度分级:
  低敏感  | 提示词配置、Skill注册表   | 直连 + 上报    | 静态/半静态，无风险
  中敏感  | 信息缓存                 | 直连 + 详细审计 | 含用户会话内容
  高敏感  | 长期存储(私密记忆)、凭证   | 经核心审批      | 泄露不可逆

设计逻辑: 数据泄露后果越严重，核心介入越深。


六、核心运行流程（单次请求完整链路）
-----------------------------------

① 接口部收到用户消息 → 归一化 → 交给核心
② 核心路由到组装部 → 完整组装路径:
   · 读提示词配置 → System Prompt
   · 读 Skill 注册表 → 工具声明 + Skill 描述
   · 读信息缓存 → 会话历史
   · 读长期存储 → 检索相关记忆
   · 组装 + 裁剪 → 完整 Context Window
   → 返回核心
③ 核心路由到模型部 → 推理 → 输出解析
④ 输出是 tool_call:
   → 核心路由到工具部 → 执行 → 结果通过核心写入信息缓存
   → 核心路由到组装部（追加路径）→ 更新 Context Window
   → 回到 ③（循环）
⑤ 输出是文本回复:
   → 核心路由到接口部 → 格式化 → 回复用户
   → 核心将本轮对话写入信息缓存
   → 核心触发会话归档（必要时写入长期存储）


七、Context Window 结构解剖
----------------------------

开始态（用户第一条消息）:
  [A] System Prompt        ← 来自提示词配置
  [B] 工具声明列表          ← 来自 Skill 注册表
  [C] 长期记忆注入          ← 来自长期存储（检索结果）
  [D] 用户消息              ← 来自接口部（归一化后）

中间态（Agent Loop 第 N 轮）:
  [A] System Prompt         （同上）
  [B] 工具声明列表           （同上）
  [C] 长期记忆注入           （同上）
  [D] 用户消息               （同上）
  [E] 模型第1轮决策          ← 循环引擎产生
  [F] 工具第1轮返回结果       ← 工具部产生，经组装部格式化
  [G] 模型第2轮决策          ← 循环引擎产生
  [H] 工具第2轮返回结果